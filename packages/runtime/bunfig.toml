[test]
coverageReporter = ["text", "lcov"]
coverageSkipTestFiles = true
coveragePathIgnorePatterns = [
  "**/tests/**",
  "./result/**",
  # Parser moved under extensions/; it resolves at ../../extensions/parser now.
  "../../extensions/parser/**",
  "../compiler/**",
  "../create/**",
  "../desktop/**",
  "../markup/**",
  "../schema/**",
  "../server/**",
  "../site/**",
  "../studio/**",
]
coverageThreshold = { lines = 0.963, functions = 0.98 }
