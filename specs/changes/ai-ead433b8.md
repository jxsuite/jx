---
spec: ai.md
level: patch
---

§2 Stop is armed before the send path's first await (the chat URL is resolved inside the first request); §3.5 a Stop later in a turn leaves a finished import's outcome as it was.
