---
spec: ui.md
level: minor
---

jx-split memoizes the resolved track's identity per element — length still measured per call, walked invalidation on containment/connectivity/axis change — after profiling traced per-boot forced recalcs to the walk
