---
spec: desktop.md
level: minor
---

§3.1 the PAL gains readFileBytes, an optional raw-bytes read, because readFile decodes UTF-8 and turns an image into replacement characters, and on the desktop the loopback asset URL taints a canvas while CORS is banned outright.
