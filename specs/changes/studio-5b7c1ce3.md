---
spec: studio.md
level: minor
---

§5.5: the sync bar and the Active branch row are wrapping rows; the remote verbs and the branch picker drop below the status when the Navigator is too narrow, and the layout list names the rows the panel actually draws.
