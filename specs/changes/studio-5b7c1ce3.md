---
spec: studio.md
level: minor
---

§5.5: the sync bar and the Active branch row are wrapping rows; the remote verbs and the branch picker take a line of their own whenever the status cannot share one, which at the Navigator's default width they already do, and the layout list names the rows and sub-tabs the panel actually draws.
