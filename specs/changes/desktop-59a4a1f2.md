---
spec: desktop.md
level: minor
---

§7.3a the shared services server gates its AI route with the per-process token the import route already required, refuses non-loopback Hosts, and advertises the bound literal 127.0.0.1.
