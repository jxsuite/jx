---
spec: studio.md
level: major
---

§3.4 pickDirectory resolves null only for a cancel and a rejection is shown under Location on every platform; §11.2 a non-empty boot writes the jx-boot marker, which turns the dev-server default off so an unregistered launcher boots into the failure state.
