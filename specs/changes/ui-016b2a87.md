---
spec: ui.md
level: minor
---

§4.3: every outset hit area is layout-contained so it adds nothing to a scrolling ancestor's overflow; a small button, action button or checkbox therefore has no baseline and is centred in a row.
