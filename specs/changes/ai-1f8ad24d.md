---
spec: ai.md
level: minor
---

§2 the usage frame carries the provider's own token count before done, and a round runs the tool calls its stream carried whatever the finish reason, except a cancelled round, which runs none.
